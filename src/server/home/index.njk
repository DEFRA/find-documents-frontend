{% extends 'layouts/page.njk' %}

{% block content %}
  {{ appHeading({
    text: heading,
    caption: "Basic upload of a file."
  }) }}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <div class="govuk-body" data-testid="app-page-body">
        <p>
          {{ govukButton({
            text: "Start now",
            href: "/basic",
            isStartButton: true
          }) }}
        </p>
      </div>
  </div>

{% endblock %}
